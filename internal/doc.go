// Package internal hosts private helpers that should not be importable
// outside this module. It carries no code of its own: the subpackages are
// where things live (roster, lint).
package internal
